<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.lantern" >

    <uses-sdk android:minSdkVersion="15" android:targetSdkVersion="23"/>

    <application
        android:name="org.lantern.LanternApp"
        android:allowBackup="true"
        android:persistent="true"
        android:icon="@drawable/lantern_icon"
        android:theme="@style/AppTheme"
        android:label="@string/app_name">

        <provider android:name="org.lantern.model.ApkProvider" android:authorities="org.lantern.gmailattach.provider" android:exported="false">
        </provider>

        <service android:name="com.google.android.gms.analytics.AnalyticsService"
            android:enabled="true"
            android:exported="false"/>


        <activity
            android:name="com.thefinestartist.finestwebview.FinestWebViewActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="sensor"
            android:theme="@style/FinestWebViewTheme.Light" />

        <activity
            android:name=".activity.LanternMainActivity_"
            android:launchMode="singleTop"
            android:configChanges="layoutDirection|locale"
            android:screenOrientation="portrait"
            android:label="@string/app_name">

            <receiver android:name=".LanternReceiver">
                <intent-filter>
                    <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
                    <action android:name="android.net.wifi.STATE_CHANGE"/>
                    <action android:name="android.intent.action.ACTION_SHUTDOWN" />
                    <action android:name="android.intent.action.SCREEN_ON" />
                    <action android:name="android.intent.action.SCREEN_OFF" />
                    <action android:name="android.intent.action.USER_PRESENT" />
                </intent-filter>
            </receiver>

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.VerifyCodeActivity_"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".activity.ReferralCodeActivity_"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".activity.WelcomeActivity_"
            android:screenOrientation="portrait"
            android:noHistory="true">
        </activity>

        <activity
            android:name=".activity.InviteActivity_"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activity.PaymentActivity_"
            android:screenOrientation="portrait"
            android:noHistory="true" >

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data 
                    android:scheme="lantern" 
                    android:path="/"
                    android:host="pro"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.CheckoutActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name" 
            android:noHistory="true" />

        <activity
            android:name=".activity.DesktopActivity_"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activity.PlansActivity_"
            android:screenOrientation="portrait"
            />

        <activity
            android:name=".activity.ProAccountActivity_"
            android:screenOrientation="portrait"
            /> 
        <activity
            android:name=".activity.LanguageActivity_"
            android:screenOrientation="portrait"
            /> 
        <activity
            android:name=".activity.SignInActivity"
            android:screenOrientation="portrait"
            />
 

        <activity
            android:name=".activity.UpdateActivity_"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Blank.Dialog">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <service android:name=".vpn.Service"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
        </service>

        <!-- ************************** Begin PubSub Handling ********************************** -->
        <!-- This receiver receives notifications from pubsub -->
        <receiver android:name=".model.Subscribe">
            <intent-filter>
                <action android:name="org.lantern.pubsub.intent.SERVICE_STARTED" />
            </intent-filter>
        </receiver>

        <!-- This receiver receives notifications from pubsub -->
        <receiver android:name=".model.Notify">
            <intent-filter>
                <action android:name="org.lantern.pubsub.intent.MESSAGE_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- This receiver allows us to subscribe to notifications upon boot so that we get them
             even if the app hasn't been opened by the user. -->
        <receiver android:name="org.lantern.pubsub.AutoStart">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- This service enables PubSub handling -->
        <service
            android:name="org.lantern.pubsub.PubSubService"
            android:exported="true"
            android:process=":PubSubService"
            />
        <!-- ************************** End PubSub Handling ************************************ -->


        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="284fdb6578c78a774887f65e1a34b9024732a29c" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
</manifest>
