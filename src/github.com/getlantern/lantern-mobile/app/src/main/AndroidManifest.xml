<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.getlantern.lantern" >

    <application
        android:name="org.getlantern.lantern.LanternApp"
        android:allowBackup="true"
        android:icon="@drawable/lantern_icon"
        android:theme="@style/AppTheme"
        android:label="@string/app_name">

        <service android:name="com.google.android.gms.analytics.AnalyticsService"
            android:enabled="true"
            android:exported="false"/>
      
        <activity
            android:name=".activity.LanternMainActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
                 <activity
                     android:name=".activity.PromptVpnActivity" android:label="@string/app_name" android:exported="false"
                     android:theme="@android:style/Theme.NoDisplay"
                     />

        <service android:name=".service.LanternVpn"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
        </service>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
</manifest>
