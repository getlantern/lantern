#!/bin/sh
set -e

UNIT="lantern.service"

case "$1" in
  remove|deconfigure)
    if command -v systemctl >/dev/null 2>&1; then
      systemctl stop "$UNIT" >/dev/null 2>&1 || true
    fi
    ;;
esac

exit 0
